# Python dependencies for the BSC-generated-Verilog-to-VHDL transpiler
# (tools/bsc2vhdl/). Install with:
#   pip install -r tools/bsc2vhdl/requirements.txt
#
# iverilog must also be on PATH: pyverilog preprocesses by shelling out to
# `iverilog -E`.
pyverilog==1.3.0
pytest==7.3.0
